"""
Deep dive structure detector verification suite (T10).

Systematic line-by-line verification of all 13 structure detectors.
Every algorithm, math formula, edge case — on hand-crafted synthetic
data AND regime-matched real SOLUSDT 1h market data.

Run: python -m src.forge.audits.structure_deep_tests.run_all
"""
