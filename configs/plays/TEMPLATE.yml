version: "3.0.0"
name: "TEMPLATE"
description: "Template Play - copy and customize"

symbol: "BTCUSDT"
tf: "15m"

features:
  ema_fast:
    indicator: ema
    params:
      length: 9
  ema_slow:
    indicator: ema
    params:
      length: 21

actions:
  entry_long:
    all:
      - ["ema_fast", ">", "ema_slow"]

risk:
  stop_loss_pct: 2.0
  take_profit_pct: 4.0
  max_position_pct: 5.0
