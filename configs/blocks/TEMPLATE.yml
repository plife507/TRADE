version: "1.0.0"
id: "TEMPLATE"
name: "Template Block"
description: "Template Block - copy and customize"

# Features this block requires (at least one)
features:
  - id: rsi
    type: indicator
    indicator_type: rsi
    params:
      length: 14

# Condition when block is active (DSL expression)
condition:
  lhs:
    feature_id: rsi
  op: lt
  rhs: 30

# Optional tags for categorization
tags:
  - template
  - example
