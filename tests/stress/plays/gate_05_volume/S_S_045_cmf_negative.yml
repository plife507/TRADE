version: "3.0.0"
name: "S_S_045_cmf_negative"
description: "Stress Gate 5: CMF negative money flow entry (short). Volume distribution signal."

symbol: "LTCUSDT"
timeframes:
  low_tf: "1h"
  med_tf: "1h"
  high_tf: "1h"
  exec: "low_tf"
account:
  starting_equity_usdt: 10000.0
  max_leverage: 1.0
  margin_mode: isolated_usdt
  min_trade_notional_usdt: 10.0
  fee_model:
    taker_bps: 6.0
    maker_bps: 1.0
  slippage_bps: 2.0

features:
  cmf_20:
    indicator: cmf
    params:
      length: 20

actions:
  entry_short:
    all:
      - ["cmf_20", "<", -0.1]
  exit_short:
    all:
      - ["cmf_20", ">", 0]

position_policy:
  mode: short_only
  exit_mode: signal
  max_positions_per_symbol: 1

risk:
  stop_loss_pct: 3.0
  take_profit_pct: 6.0
  max_position_pct: 10.0

_test_metadata:
  gate: 5
  complexity_pct: 25
  category: "volume"
  direction: "short"
