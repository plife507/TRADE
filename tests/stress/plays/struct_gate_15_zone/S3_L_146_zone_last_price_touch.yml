version: 3.0.0
name: S3_L_146_zone_last_price_touch
description: "Zone Structure: last_price touches demand zone (1m granularity entry)."
symbol: BTCUSDT
tf: 15m

account:
  starting_equity_usdt: 10000.0
  max_leverage: 1.0
  margin_mode: isolated_usdt
  min_trade_notional_usdt: 10.0
  fee_model:
    taker_bps: 6.0
    maker_bps: 1.0
  slippage_bps: 2.0

features:
  ema_21:
    indicator: ema
    params:
      length: 21

structures:
  exec:
    - type: swing
      key: swing
      params:
        left: 5
        right: 5
    - type: zone
      key: demand
      depends_on:
        swing: swing
      params:
        zone_type: demand
        width_atr: 1.5

actions:
  entry_long:
    all:
      # Zone is active
      - [{feature_id: demand, field: state}, "==", "ACTIVE"]
      # last_price (1m ticker) touches demand zone upper boundary
      - [last_price, "near_pct", {feature_id: demand, field: upper}, 0.5]
      # Basic trend filter
      - [last_price, ">", ema_21]
  exit_long:
    any:
      # Zone broken
      - [{feature_id: demand, field: state}, "==", "BROKEN"]
      # Price drops significantly
      - [last_price, "<", {feature_id: demand, field: lower}]

position_policy:
  mode: long_only
  exit_mode: signal
  max_positions_per_symbol: 1

risk:
  stop_loss_pct: 3.0
  take_profit_pct: 6.0
  max_position_pct: 10.0

_test_metadata:
  gate: 15
  complexity_pct: 60
  category: zone
  direction: long
  tests:
    - last_price_zone_interaction
    - 1m_granularity_entry
    - live_trading_parity
