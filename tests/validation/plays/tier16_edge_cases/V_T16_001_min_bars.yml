version: "3.0.0"
name: "V_T16_001_min_bars"
description: "Minimum warmup bars test - correct warmup handling"

synthetic:
  pattern: "trend_up_clean"
  bars: 100
  seed: 1601

symbol: "BTCUSDT"
timeframes:
  low_tf: "15m"
  med_tf: "1h"
  high_tf: "4h"
  exec: "low_tf"

account:
  starting_equity_usdt: 10000.0
  max_leverage: 1.0
  min_trade_notional_usdt: 10.0
  fee_model:
    taker_bps: 5.5
    maker_bps: 2.0

features:
  ema_5:
    indicator: ema
    params:
      length: 5
  ema_10:
    indicator: ema
    params:
      length: 10

actions:
  entry_long:
    all:
      - ["ema_5", "cross_above", "ema_10"]

position_policy:
  mode: long_only
  exit_mode: sl_tp_only

risk:
  stop_loss_pct: 2.0
  take_profit_pct: 4.0
