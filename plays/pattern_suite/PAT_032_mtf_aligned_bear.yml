version: "3.0.0"
name: "pat_032_mtf_aligned_bear"
description: |
  Pattern Suite - Synthetic pattern 'mtf_aligned_bear' coverage test.
  Simple EMA crossover strategy to verify engine handles this pattern.

symbol: "BTCUSDT"

timeframes:
  low_tf: "15m"
  med_tf: "1h"
  high_tf: "D"
  exec: "low_tf"

account:
  starting_equity_usdt: 10000.0
  max_leverage: 1.0
  margin_mode: "isolated_usdt"
  min_trade_notional_usdt: 10.0
  fee_model:
    taker_bps: 5.5
    maker_bps: 2.0
  slippage_bps: 2.0

features:
  ema_9:
    indicator: ema
    params: {length: 9}

  ema_21:
    indicator: ema
    params: {length: 21}

  rsi_14:
    indicator: rsi
    params: {length: 14}

structures:
  exec: []

actions:
  entry_short:
    all:
      - ["ema_9", "cross_below", "ema_21"]

position_policy:
  mode: "short_only"
  exit_mode: "sl_tp_only"
  max_positions_per_symbol: 1

risk:
  stop_loss_pct: 3.0
  take_profit_pct: 6.0
  max_position_pct: 100.0
